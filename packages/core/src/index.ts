/**
 * @craftbit/core — framework-free document model, schemas, commands, expressions,
 * units, and migrations (spec §6, §7.9, §7.14). Must never import React or Three.js.
 *
 * M0 scope: package scaffolding only. The document model (§6.2) lands in M1.
 */

export const CRAFTBIT_FORMAT_VERSION = 1 as const;
